import { ThemeOptions } from '@material-ui/core/styles';

export type ThemeDictionaryType = {
    light?: ThemeOptions;
    dark?: ThemeOptions;
    deuteranopia?: ThemeOptions;
    tritanopia?: ThemeOptions;
  };
